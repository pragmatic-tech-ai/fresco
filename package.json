{
  "name": "@pragmatic-tech-ai/fresco",
  "version": "0.4.1",
  "description": "Graph visualization library built on top of mural",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "access": "public"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "ge": "tsx src/main.ts",
    "ge:compound": "tsx src/compound-demo.ts",
    "test": "tsx --test \"src/**/*.test.ts\"",
    "clean": "rimraf dist",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/node": "^22.10.0",
    "rimraf": "^6.0.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@pragmatic-tech-ai/mural": "^0.45.0",
    "yaml": "^2.9.0"
  }
}
